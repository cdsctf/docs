# Concept

Unlike most CTF platforms, CdsCTF separates **challenges** from **games**. Challenges are managed independently; games are competitions that reference a subset of challenges. This allows a clear split between practice mode (playing challenges directly) and competition mode (participating in timed games with rankings).
