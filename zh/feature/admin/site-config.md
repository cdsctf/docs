# 站点配置

以下配置在**管理后台**中设置，并持久化在数据库中（不写入 `config.toml`）。请在部署完成后通过管理界面进行设置。

## 验证码

支持 hCaptcha、Cloudflare Turnstile 或内置图片/POW 验证码，以及难度等相关选项。

## 邮件

配置发件服务器与模板，用于账号验证、找回密码等场景。

## 站点 Logo

用于前台展示的 Logo 图片，在管理后台中上传与更换。
